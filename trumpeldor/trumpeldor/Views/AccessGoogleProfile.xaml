<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="trumpeldor.Views.AccessGoogleProfile"
             xmlns:translator="clr-namespace:trumpeldor.Helpers">
    <ContentPage.Content>
        <StackLayout Style="{DynamicResource mainStackLayout}">
            <!--<Button Text="{translator:Translate login_with_google}" x:Name="googleLoginButton" x:Uid="googleLoginButton" Style="{StaticResource largeButtonStyle}"/>-->
            <!--<Button Text="{translator:Translate login_with_google}" x:Name="aaaaaa" x:Uid="aaaaaa" Style="{StaticResource largeButtonStyle}"/>-->
        </StackLayout>
    </ContentPage.Content>
</ContentPage>